{
  "name": "@quorum/db",
  "version": "0.0.1",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:migrate:dev": "bunx prisma migrate dev",
    "db:migrate:deploy": "bunx prisma migrate deploy",
    "db:generate": "bunx prisma generate",
    "db:studio": "bunx prisma studio",
    "db:push": "bunx prisma db push",
    "db:seed": "bun run src/seed.ts",
    "test": "bun run src/test-connection.ts",
    "verify": "bun run src/verify.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}
