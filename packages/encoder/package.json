{
	"name": "@quorum/encoder",
	"version": "0.0.0",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "bun build src/index.ts --outdir dist --target bun",
		"test": "bun test",
		"typecheck": "tsc --noEmit"
	},
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"dependencies": {
		"@quorum/shared": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "latest"
	}
}
